<nav class="navbar navbar-expand-lg navbar-custom">
  <div class="container">
    
    <!-- STAYORA BRAND LOGO -->
    <a class="navbar-brand navbar-brand-custom" href="/listings">
      <div class="stayora-logo-icon">
        <i class="fa-solid fa-house-chimney-crack"></i>
      </div>
      <div class="d-flex flex-column leading-none">
        <span class="fw-extrabold" style="letter-spacing: -0.04em;">stayora</span>
      </div>
    </a>

    <!-- Mobile Toggler -->
    <button class="navbar-toggler border-0 shadow-none p-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <i class="fa-solid fa-bars fs-5"></i>
    </button>

    <!-- Center Search Capsule (Desktop) -->
    <div class="d-none d-lg-flex mx-auto">
      <form action="/listings/search" method="GET" class="nav-search-bar">
        <input type="text" name="country" class="nav-search-input" placeholder="Search by country, city, or stay..." value="<%= country || '' %>" required>
        <button class="nav-search-btn" type="submit" aria-label="Search">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>
    </div>

    <!-- Right Navigation Actions -->
    <div class="collapse navbar-collapse flex-grow-0" id="navbarContent">
      
      <!-- Mobile Search Form -->
      <div class="d-lg-none my-3">
        <form action="/listings/search" method="GET" class="d-flex gap-2">
          <input type="text" name="country" class="form-control rounded-pill px-3" placeholder="Search destination..." value="<%= country || '' %>" required>
          <button class="btn btn-primary-custom rounded-circle p-0" style="width: 40px; height: 40px;" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </form>
      </div>

      <ul class="navbar-nav align-items-lg-center gap-lg-3 ms-auto text-center text-lg-start">
        
        <li class="nav-item">
          <a class="nav-link fw-semibold text-dark" href="/listings">Explore</a>
        </li>

        <li class="nav-item">
          <a class="nav-link fw-semibold text-dark" href="/listings/new">
            <span class="d-inline-flex align-items-center gap-1">
              <i class="fa-solid fa-house-chimney text-muted"></i>
              Host your stay
            </span>
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link fw-semibold text-dark" href="/wishlist" id="nav-wishlist-link">
            <span class="d-inline-flex align-items-center gap-1">
              <i class="fa-regular fa-heart text-danger"></i>
              Wishlist
              <span class="badge bg-danger rounded-pill ms-1 <%= (typeof wishlistCount !== 'undefined' && wishlistCount > 0) ? '' : 'd-none' %>" id="wishlist-count-badge">
                <%= (typeof wishlistCount !== 'undefined' && wishlistCount > 0) ? wishlistCount : 0 %>
              </span>
            </span>
          </a>
        </li>

        <% if (currUser) { %>
          <!-- User Dropdown Menu -->
          <li class="nav-item dropdown ms-lg-2 mt-2 mt-lg-0">
            <a class="user-menu-pill dropdown-toggle text-decoration-none" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fa-solid fa-bars text-muted me-1"></i>
              <div class="user-avatar">
                <%= currUser.username ? currUser.username[0].toUpperCase() : 'U' %>
              </div>
            </a>
            
            <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 rounded-4 mt-2 p-2" style="min-width: 230px;">
              <li class="px-3 py-2 border-bottom mb-1">
                <div class="fw-bold text-dark"><%= currUser.username %></div>
                <small class="text-muted"><%= currUser.email %></small>
              </li>
              <li>
                <a class="dropdown-item py-2 rounded-2 fw-semibold" href="/wishlist">
                  <i class="fa-solid fa-heart me-2 text-danger"></i> Saved Wishlist
                </a>
              </li>
              <li>
                <a class="dropdown-item py-2 rounded-2 fw-semibold" href="/bookings/my-bookings">
                  <i class="fa-solid fa-suitcase me-2 text-primary"></i> Your Reservations
                </a>
              </li>
              <li>
                <a class="dropdown-item py-2 rounded-2 fw-semibold" href="/bookings/host">
                  <i class="fa-solid fa-chart-line me-2 text-success"></i> Your Stays (Host Dashboard)
                </a>
              </li>
              <li>
                <a class="dropdown-item py-2 rounded-2 fw-semibold" href="/listings/new">
                  <i class="fa-solid fa-plus-circle me-2 text-warning"></i> Add New Stay
                </a>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <form action="/logout" method="POST" class="d-inline">
                  <button type="submit" class="dropdown-item py-2 rounded-2 fw-semibold text-danger">
                    <i class="fa-solid fa-arrow-right-from-bracket me-2"></i> Log out
                  </button>
                </form>
              </li>
            </ul>
          </li>

        <% } else { %>
          <li class="nav-item ms-lg-2 mt-2 mt-lg-0 d-flex gap-2 justify-content-center">
            <a class="btn btn-outline-custom rounded-pill px-3 py-2 text-dark" href="/login">Log in</a>
            <a class="btn btn-primary-custom rounded-pill px-3 py-2" href="/signup">Sign up</a>
          </li>
        <% } %>

      </ul>
    </div>

  </div>
</nav>

<style>
  .stayora-logo-icon {
    width: 38px;
    height: 38px;
    background: linear-gradient(135deg, #FF385C 0%, #E00B41 100%);
    color: #FFFFFF;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.15rem;
    box-shadow: 0 4px 10px rgba(255, 56, 92, 0.3);
  }
</style>
